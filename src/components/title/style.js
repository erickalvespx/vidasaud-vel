//importação de componentes
import { StyleSheet } from "react-native";

//estilização do texto do aplicativo!
const styles = StyleSheet.create({
   boxTitle:{
    alignItems: "center",
    justifyContent: "center",
    padding: 10,
   },
   textTitle:{
    color: "#000000",
    fontSize: 24,
    fontWeight: "bold",
   }
});

export default styles